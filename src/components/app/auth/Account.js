import React, { Component } from 'react';
import Layout from '../../layout';
import SEO from '../../seo';

export default class Account extends Component {

    render() {
        return (
            <Layout>
                <SEO title="Użytkownik"/>
                
            </Layout>
        );
    }
}
